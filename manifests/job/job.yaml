apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
spec:
  template:
    spec:
      containers:
        - name: hello
          image: busybox:stable
          command: ["sh", "-c", "echo 'Hello from Kubernetes'"]
      restartPolicy: Never